---
- name: Install HTTPD and Start HTTPD
  remote_user: ansible
  hosts: all
  become: root
  ignore_errors: yes
  
  tasks:

  - name: Install HTTPD
    yum:
      name: httpd
      state: present

  - name: Start the HTTPD
    service:
      name: httpd
      state: started
